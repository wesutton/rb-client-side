import React from 'react';

function MessageAdd(props){

    // const {handleCommentSubmit} = props;
    // let message = '';
    return (
        <div>
            {/* <div className = "card mt-4 mb-3">
                <div className="card-header"><strong>Comments</strong></div>
                <div className="card-body">
                    <textarea name="comment" className="form-control" placeholder="Add a new comment" onChange={event => comment = event.target.value}>
                    </textarea>
                </div>
            </div> 
            <div>
                <button className="btn btn-primary mr-3" onClick={event =>handleCommentSubmit((message))}>Comment</button>
            </div> */}
        </div>
           
    )
}

export default MessageAdd;